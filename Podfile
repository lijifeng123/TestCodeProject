# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TTT' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

#  pod 'manager', :path => 'manager'
  
  # Pods for TTT

  target 'TTTTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TTTUITests' do
    # Pods for testing
  end

end
